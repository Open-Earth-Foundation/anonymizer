import json


def export_to_db(data):
    # Write output to database
    print(f"Writing output with {len(data)} threads to database...")

    # Connect to the database

    # Write data to the database
